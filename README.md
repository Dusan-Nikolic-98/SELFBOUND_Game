# SELFBOUND_Game
Small retro style game.

## Run locally

```text
npm install
npm run build
```

Serve the repository root with a local static server and open `index.html` in a desktop browser. The compiled browser entrypoint is `dist/main.js`.

Checks:

```text
npm run typecheck
npm test
```
